import { CommercialPartFilterPipe } from './commercial-part-filter.pipe';

describe('CommercialPartFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CommercialPartFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
