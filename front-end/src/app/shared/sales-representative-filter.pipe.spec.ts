import { SalesRepresentativeFilterPipe } from './sales-representative-filter.pipe';

describe('SalesRepresentativeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SalesRepresentativeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
